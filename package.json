{
  "name": "solaudit-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "cli",
    "web"
  ],
  "scripts": {
    "test": "cd packages/core && bun test",
    "build:cli": "cd cli && bun run build",
    "dev:web": "cd web && bun run dev",
    "build:web": "cd web && bun run build"
  },
  "license": "MIT"
}
