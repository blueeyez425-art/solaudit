# Optional. Without this, GitHub repo scans use unauthenticated API requests,
# which are capped at 60 requests/hour per IP. Adding a token (needs no
# special scopes — public_repo read access is enough) raises that to 5,000/hour.
GITHUB_TOKEN=
